# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Arbiter runtime artifacts — never commit customer/demo state
*.db
*.db-journal
audit.jsonl

# Local tool state
.claude/
.DS_Store

# Front end build tooling (frontend/ is being rebuilt from scratch after
# arbiter/web/ was removed — no build-output path decided yet).
frontend/node_modules/
frontend/.vite/
