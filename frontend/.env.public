# Loaded automatically by `vite build --mode public` (see package.json's
# build:public script). Never set this for a customer-install build — see
# docs/LANDING-BRIEF.md §1 and AGENTS.md constraint 5.
VITE_PUBLIC_LANDING=true
